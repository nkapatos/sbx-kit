# sbx-kit CLI

Go sources. `sbx-kit --help` and `sbx-kit concepts` are the reference.

```bash
cd cli && go build -o ../bin/sbx-kit ./cmd/sbx-kit
export SBX_TREE=/path/to/this/repo
../bin/sbx-kit version
```
