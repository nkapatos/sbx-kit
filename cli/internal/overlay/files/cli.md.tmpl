# sbx-kit CLI overlay

Generated by **sbx-kit {{ .Version }}** (sbx >= {{ .MinSbx }}). Not a user kit.

**Box agent:** start at `/etc/sbx-kit/context.md` (overlay index). Then
`/etc/sbx-kit/floor.md` for this image. User kit `agentContext` is extra.

## Host commands

```bash
sbx-kit box run <dir>/<name> --yes
sbx-kit box check
sbx-kit box bindings
sbx-kit box state export | import
sbx-kit box upgrade
sbx-kit box rm --keep-state
sbx-kit recipes verify <dir>/<name>
```

## This box

- Index: `/etc/sbx-kit/context.md`
- Floor probe: `/etc/sbx-kit/floor.md`
- Portable state: `/home/agent/.local/share/sbx-kit/portable/`
- Pack helper: `sbx-kit-state` (`pack` / `unpack` / `check`)
