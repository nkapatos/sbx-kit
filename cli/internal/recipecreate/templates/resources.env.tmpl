# Resource flags passed to `sbx` at create. Override with the same env names
# or DOCKER_SANDBOXES_ROOT_SIZE / DOCKER_SANDBOXES_DOCKER_SIZE.
SBX_MEMORY=4g
SBX_CPUS=2
SBX_ROOT_SIZE=20g
SBX_DOCKER_SIZE=40g
