# Absolute path to the project bind-mounted at /workspace (required on VPS)
WORKSPACE=/home/cursor/workspaces/my-app

# Model provider key (never commit; chmod 600 this file)
ANTHROPIC_API_KEY=

# Optional resource caps (compose defaults: 2g / 2 cpus)
# MEM_LIMIT=2g
# CPUS=2.0
