# Agent workplace kit (stub)

Runtime-agnostic **mixin** for MCP / skills / plugins conventions inside the sandbox.

Host `~/.cursor` is **not** available in sbx. Prefer:

1. Project-stamped README / conventions via `sbx-kit init`
2. This kit for allowlists + shared skill packs + short agentContext

Pair with `mise-workspace` on any mise-prepared template. Implementation TBD.
