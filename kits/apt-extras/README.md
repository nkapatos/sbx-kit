# Apt extras

Optional mixin for small apt packages in the Linux VM (not host Homebrew).
Preference CLIs (`gh`, `glab`, …) belong here or in a team fork — not in
kit-core. Languages stay on mise.

Add it on a recipe in `config/agents.yaml`.
