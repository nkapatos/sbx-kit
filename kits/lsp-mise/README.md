# LSP / editor helpers (mise)

Optional mixin: box-level `/mise/config.toml` (created if missing). Does not
replace project `mise.toml` or run `mise install`. Pair with `mise-workspace`.

Add it on a recipe in `config/agents.yaml`.
