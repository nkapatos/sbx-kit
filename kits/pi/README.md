# Pi

Sandbox kit for [Pi](https://pi.dev). Kind is `pi`, not `shell`.

Recipes: `sbx-kit recipes` (`pi` on Hub shell, `kit-pi` on kit-shell).
Install and credentials are in `spec.yaml`. Host: `sbx secret set <service>`
for any providers you use (`sbx-kit run` lists them).
