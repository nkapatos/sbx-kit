# kit-shell

FROM kit-core. Tini as PID 1 + login bash. No extra packages.

Hub-`shell` counterpart: this image is the basis; add **kits**. Baked agents
FROM kit-core, not this image.

`sbx-kit image load --engine docker kit-shell`
