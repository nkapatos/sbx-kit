# Template: pi-mise-docker — BROKEN / unused

Parked with the Pi kit. Do not load for `sbx-kit run --agent pi` (recipe is
stubbed). Next work is plain Docker/Compose for VPS, not this layered sbx
template.
